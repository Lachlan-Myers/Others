package controller;

import java.util.regex.Matcher;

import model.Station;

import org.w3c.dom.Document;
import org.w3c.dom.Node;

public interface WeatherPropertyCallback {
	/**
	 * Callback function to allow client class to handle setting a node's dynamic value.
	 * @param d Document the node is contained within.
	 * @param n Node that should be changed.
	 * @param propName Property name that the node contains.
	 */
	public void weatherPropertyCallback(Document d, Node n, String propName);
}
